class Solution
{
  public:
  int duplicates(vector<int>& nums)
  {
    sort(nums.begin(),nums.end());
    int n=nums.size();
    int i=0;
    for(int j=1;j<n;j++)
    {
      if(nums[j]!=nums[i])
      {
        i++;
        nums[i]=nums[j];
        }
      }
    return i+1;
    }
  }
