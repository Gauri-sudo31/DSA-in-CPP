class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        int sum=nums[0];
        int max1=nums[0];
        for(int i=1;i<nums.size();i++)
        {
            sum=max(nums[i],sum+nums[i]);
            if(sum>max1)
            max1=sum;
        }
        return max1;
    }
};
